export const DEFAULT_SOCIAL_IMAGE = {
  alt: "Afterglow, terminal UI components for shadcn projects",
  height: 630,
  url: "/opengraph-image",
  width: 1200,
};

export function socialTitle(title: string): string {
  return `${title} | Afterglow`;
}
